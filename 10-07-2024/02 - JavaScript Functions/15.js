// Write a JavaScript function to compute the value of bn where n
// is the exponent and b is the bases. Accept b and n from the user and display the result.

function computeExponent(b, n) {
  return b ** n;
}

console.log(computeExponent(2, 3));
